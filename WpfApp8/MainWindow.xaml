<Window x:Class="WpfApp8.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp8"
        mc:Ignorable="d"
        Background="#FFFDF9"
        FontFamily="Freestyle Script"
        FontSize="26"
        Foreground="#4C342A"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:Animal/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1.2*"/>
            <RowDefinition Height="6*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.3*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1.3*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.ColumnSpan="3" Text="Zoo" FontSize="60" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="1">
            <TextBlock Text="Name: " Margin="0,0,0,10" HorizontalAlignment="Center"/>
            <TextBlock Text="Birth: " Margin="0,0,0,10" HorizontalAlignment="Center"/>
            <TextBlock Text="Type: " Margin="0,0,0,10" HorizontalAlignment="Center"/>
            <TextBlock Text="Gender: " Margin="0,0,0,10" HorizontalAlignment="Center"/>
            <TextBlock Text="Weight: " Margin="0,0,0,10" HorizontalAlignment="Center"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="1" >
            <TextBox x:Name="name" Validation.ErrorTemplate="{StaticResource errorMessage}" Width="220" Height="30" Margin="0,0,0,10" HorizontalAlignment="Left">
                <TextBox.Text>
                    <Binding Path="Name" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <local:MinLength MinLen="3"/>
                            <local:MaxLength MaxLen="10"/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <TextBox x:Name="birth" Validation.ErrorTemplate="{StaticResource errorMessage}" Width="220" Height="30" Margin="0,0,0,10" HorizontalAlignment="Left">
                <TextBox.Text>
                    <Binding Path="Birth" UpdateSourceTrigger="PropertyChanged" TargetNullValue="">
                        <Binding.ValidationRules>
                            <local:Birth />
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <TextBox x:Name="type" Validation.ErrorTemplate="{StaticResource errorMessage}" Width="220" Height="30" Margin="0,0,0,10" HorizontalAlignment="Left">
                <TextBox.Text>
                    <Binding Path="Type" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <local:MinLength MinLen="3"/>
                            <local:MaxLength MaxLen="10"/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <TextBox x:Name="gender" Validation.ErrorTemplate="{StaticResource errorMessage}" Width="220" Height="30" FontSize="22" Margin="0,0,0,10" HorizontalAlignment="Left">
                <TextBox.Text>
                    <Binding Path="Gender" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <local:Gender/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <TextBox x:Name="weight" Validation.ErrorTemplate="{StaticResource errorMessage}" Width="220" Height="30" Margin="0,0,0,10" HorizontalAlignment="Left">
                <TextBox.Text>
                    <Binding Path="Weight" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <local:Weight/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <Button Content="Submit" Background="#C19A6B" Click="Submit" Width="100" Height="40" HorizontalAlignment="Left"/>
        </StackPanel>
        <Button Content="Show" Background="#C19A6B" Click="Show" Grid.Row="2" Grid.Column="2" Width="100" Height="40" VerticalAlignment="Top" HorizontalAlignment="Center"/>
        <WrapPanel Margin="50" Grid.Row="2" Grid.Column="2" x:Name="showAnimals"/>
    </Grid>
</Window>
