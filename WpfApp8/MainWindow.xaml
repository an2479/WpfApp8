<Window x:Class="WpfApp8.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp8"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:Animal/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition Width="1.3*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.ColumnSpan="3" Text="Zoo" FontSize="60" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="1" Grid.Column="1">
            <TextBlock Text="Name: " FontSize="22" Margin="0,0,0,10" HorizontalAlignment="Left"/>
            <TextBlock Text="Birth: " Margin="0,0,0,10" FontSize="22" HorizontalAlignment="Left"/>
            <TextBlock Text="Type: " FontSize="22" Margin="0,0,0,10" HorizontalAlignment="Left"/>
            <TextBlock Text="Gender: " FontSize="22" Margin="0,0,0,10" HorizontalAlignment="Left"/>
            <TextBlock Text="Weight: " FontSize="22" Margin="0,0,0,10" HorizontalAlignment="Left"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="2" >
            <TextBox x:Name="name" Width="220" Height="30" FontSize="22" Margin="0,0,0,10" HorizontalAlignment="Left">
                <TextBox.Text>
                    <Binding Path="Name" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <local:MinLength MinLen="3"/>
                            <local:MaxLength MaxLen="10"/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <!--<DatePicker x:Name="birthPicker" 
                SelectedDate="{Binding Birth, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}">
            </DatePicker>-->
            <TextBox x:Name="birth" Width="220" Height="30" FontSize="22" Margin="0,0,0,10" HorizontalAlignment="Left">
                <TextBox.Text>
                    <Binding Path="Birth" UpdateSourceTrigger="PropertyChanged" TargetNullValue="">
                        <Binding.ValidationRules>
                            <local:Birth />
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <TextBox x:Name="type" Width="220" Height="30" FontSize="22" Margin="0,0,0,10" HorizontalAlignment="Left">
                <TextBox.Text>
                    <Binding Path="Type" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <local:MinLength MinLen="3"/>
                            <local:MaxLength MaxLen="8"/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <TextBox x:Name="gender" Width="220" Height="30" FontSize="22" Margin="0,0,0,10" HorizontalAlignment="Left">
                <TextBox.Text>
                    <Binding Path="Gender" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <local:MinLength MinLen="1"/>
                            <local:MaxLength MaxLen="1"/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <TextBox x:Name="weight" Width="220" Height="30" FontSize="22" Margin="0,0,0,10" HorizontalAlignment="Left">
                <TextBox.Text>
                    <Binding Path="Weigh" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <local:Weight/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <Button Content="Submit" Click="Submit" Width="100" Height="40" FontSize="22" HorizontalAlignment="Left"/>
        </StackPanel>
        <Button Grid.Row="1" Click="Show" Content="Show" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Top"/>
        <!--<ListBox  FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" Width="250" Height="250"/>-->
        <StackPanel Margin="70" Grid.Row="1" x:Name="showAnimals"/>
    </Grid>
</Window>
